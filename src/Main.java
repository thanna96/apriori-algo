public class Main {
    public static void main(String[] args) {
        // Entry point for running experiments or demos.
        // You can instantiate your DB and miners here, e.g.:
        // TransactionDB db = new TransactionDB("data/transactions.txt");
        // Apriori apriori = new Apriori(db);
        // apriori.run(0.3);
    }
}
